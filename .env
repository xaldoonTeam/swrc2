# Backend API URL (no trailing slash). Used when the frontend fetches data.
# In development: run the API with `npm run dev` from the server folder (default: http://localhost:3001).
VITE_API_URL=http://localhost:3001
